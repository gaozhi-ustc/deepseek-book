# CLAUDE.md — DeepSeek 书稿（第2/3章）项目交接说明

本仓库是 DeepSeek 技术书稿第2章、第3章的写作仓库（GitHub: `gaozhi-ustc/deepseek-book`，主分支 `main`）。
本文件记录截至 2026-07-14 的全部改动细节、编辑指导原则和工具链用法，供接手同事（人或 AI 助手）快速上手。

## 一、仓库文件说明

| 文件/目录 | 说明 |
|---|---|
| `chapter2_new.md` | 第2章《技术原理解析》正文（约 2589 行），已按二审目录完成重构 |
| `chapter3_new.md` | 第3章《工程实现分析》正文（约 2471 行），已按二审目录完成重构 |
| `list2-3.md` | 两章目录二审稿（依据编辑高婧雅 2026-07-13 一审意见整理），**正文标题与此目录逐条一致**，改标题必须两处同步 |
| `RULE.md` | 编辑提出的写作原则（10 条硬性要求），改正文或目录前必读 |
| `md2docx.py` | Markdown → Word 转换脚本，LaTeX 公式转为 Word 原生公式（详见下文"工具链"） |
| `pic/` | 第2章插图 `2-1.png`～`2-4.png`（已按重构后的图号重命名） |
| `typora-user-images/` | 第3章正文引用的截图 |
| `2-3章目录-一审.docx` | 编辑一审批注原件（`*.docx` 被 gitignore，仅存本地，**勿删**） |
| `李明宇 第2章（0617）.docx` | 作者原稿（本地文件，勿删） |
| `chapter2_new.docx` / `chapter3_new.docx` | `md2docx.py` 的构建产物，gitignore 不入库，可随时重新生成 |

## 二、编辑指导原则（高婧雅一审，硬性要求，后续所有修改必须遵守）

写作四原则：**主线清晰、内容模块化、标题简洁、语义完整**。

完整的 10 条硬性要求见 **`RULE.md`**（单一出处，勿在此重复维护）。改动正文或目录前先读 RULE.md。

## 三、改动历史（按提交，从旧到新）

### 45499c2 — 整合 V4 内容重组第2/3章，第2章 docx 转为 Markdown
- 用 pandoc 把作者第2章 docx 原稿转为 `chapter2_new.md`。
- 按 DeepSeek V4 技术报告内容对两章做第一轮重组。

### f3cedef — 整合 DSpark 正式稿至 3.5，第2章 V4 综述并入 2.1，新增一审修订目录
- lmxxf 老师的 DSpark 正式书稿（源文件在 `ai-theorys-study` 仓库 `wechat/books/20260505/chapter3-dspark.md`）整合进 3.5 节。
  **注意**：DSpark 是**投机解码推理框架**（半自回归生成 + 置信度调度验证），不是训练框架——此前占位稿的定位是错的，已全部替换。
- 第2章原独立的 V4 综述并入 2.1。
- 依据编辑一审 docx 批注生成修订版目录。

### 34c81ad — 按二审目录重构第2/3章正文，新增 md2docx 转换脚本（当前 HEAD）

这是最大的一次重构，两章正文全部按 `list2-3.md` 二审目录重排，细节如下。

**第2章结构（重构后）**
- 2.1 DeepSeek V4 架构全景、训练与设计哲学（2.1.1 模型定位与参数规格 / 2.1.2 整体架构组成 / 2.1.3 训练数据、策略与数值精度 / 2.1.4 设计哲学）
- 2.2 前馈网络架构（MoE 架构原理 / MoE 负载均衡方案）
- 2.3 注意力机制原理（MLA 原理分析 / DSA 原理分析）
- 2.4 深度推理能力的构建（多词元预测训练 / R1 训练过程 / 推理能力的迁移与融合）
- 2.5 CSA/HCA 原理与混合应用（需求与瓶颈分析 / CSA 方案原理 / HCA 方案原理 / CSA 与 HCA 的混合方案）
- 2.6 mHC：基于流形约束的超连接架构优化（架构设计 / 实验验证与性能分析 / 数学性质分析 / 局限性分析）
- 2.7 Muon：基于矩阵正交化的优化器（既有优化器的瓶颈分析 / 矩阵正交化更新原理 / DeepSeek V4 中的应用 / 局限性与工程应对）

**第2章删除的内容**（都在 git 历史里，作者若要恢复可从 f3cedef 找回）
- "架构演进关键节点"时间线表及旧图2-1（违反"不叙史"）
- HC 在 OLMo 上的 4 张实验表（旧表2-13～2-16），压缩为一段文字
- 所有"本节小结"；其中不重复的实质内容已并入正文（如低维流形洞见段并入 2.5.4 效率分析结尾）
- "## 参考文献"节（26 条）→ 全部改为脚注

**第2章其他改动**
- 旧方案回顾按原则并入现行方案分析：NSA 并入 2.5 混合方案；HC 原始公式并入 2.6.1.1"约束对象"；SGD/动量/Adam/AdamW 并入 2.7.1 瓶颈分析（用加粗引导词分段）。
- 表格重编号为 表2-1～表2-20（连续无缺号）；图重编号为 图2-1～图2-4，`pic/` 下图片文件同步 git mv 重命名。
- 24 个脚注（`[^dsv4]`、`[^swiglu]`、`[^muon]` 等），定义集中在文件末尾。
- 修复了原稿中一个损坏的 MoE 门控公式（docx 转换残留的乱码 LaTeX），重写为规范形式。

**第3章结构（重构后）**
- 3.1 训练和推理工程基础设施（DeepGEMM / 3FS）
- 3.2 MoE 的工程优化（DeepEP / EPLB）
- 3.3 注意力机制的工程实现（FlashAttention 算法思路分析 / FlashMLA 实现思路分析）
- 3.4 强化学习算法的工程实现（GRPO 原理与工程挑战 / Unsloth、NeMo-RL、SWIFT 三家实现 / GRPO 算法变体）
- 3.5 DSpark 原理与技术解析（投机解码原理与技术路线 / 半自回归生成架构 / 置信度调度验证 / 训练方法 / 离线实验与分析 / DeepSeek-V4 中的线上部署 / 配套开源代码库 DeepSpec）

**第3章删除的内容**
- 3.4.6"工程实践总结"（旧表3-47/48/49，GRPO 超参对比、常见问题表）
- 3.5.8"本节小结"（旧表3-50）
- "## 参考文献"节（56 条）→ 全部改为脚注

**第3章其他改动**
- 表格重编号为 表3-1～表3-45；顺带修复了原稿"表3-21 直接跳到表3-23"的既有缺号。
- 32 个脚注。
- 修复失效交叉引用（如指向已不存在编号的"见 2.3.2.2 节"改为文字指引）。
- 统一 DAPO 全称为 Decoupled Clip and Dynamic sAmpling Policy Optimization（原稿表格与正文不一致）。

**措辞决定（用户/编辑已拍板，不要改回）**
- 3.5 标题用"DSpark 原理与技术解析"；3.5.6 用"线上部署"（不用"生产部署"）。
- 2.1 标题"架构全景、训练与设计哲学"是在编辑原措辞基础上的扩展，**尚待编辑复核**。

## 四、工具链与常用命令

### Markdown → Word（交稿用）

```bash
python3 md2docx.py                    # 默认转换 chapter2_new.md 和 chapter3_new.md
python3 md2docx.py 某文件.md          # 转换指定文件
```

- 关键点：用 pandoc 的 `gfm+tex_math_dollars` 读取器，`$...$` / `$$...$$` 中的 LaTeX 公式输出为 **Word 原生 OMML 公式**（可双击编辑），不是图片。
- 脚本自带校验：比对源文件公式数与 docx 内 `<m:oMath>` 节点数，数目不符时报警退出码 1。若报警，通常是某个公式的 LaTeX 语法 pandoc 解析不了，看 pandoc 警告定位。
- Markdown 脚注自动转为 Word 页脚注；`pic/` 相对路径图片自动嵌入。

### Word 审阅稿 → Markdown（收编辑意见用）

```bash
pandoc 审阅稿.docx -t markdown --track-changes=all -o review.md
```

`--track-changes=all` 会把编辑的插入/删除/批注提取为带标记的 span，能精确区分"编辑直接改的"（照抄）和"编辑批注建议的"（按原则执行）。

### pandoc 位置

系统未装 pandoc，静态二进制在 `~/.local/bin/pandoc`（3.6.3）。`md2docx.py` 按 `$PANDOC` 环境变量 → PATH → `~/.local/bin/pandoc` 顺序查找。

## 五、环境注意事项（本机 gaozhi-lagos）

- 代理拦截 22 端口，GitHub SSH 已配置走 443 端口（`~/.ssh/config`），`git push` 可直接用。
- 没有 `gh` CLI；GitHub 操作走 git 命令。
- git 身份是**仓库级**配置（gaozhi <qinglin890@gmail.com>），新克隆需重新配置。

## 六、遗留事项

1. **2.1 标题**（"DeepSeek V4 架构全景、训练与设计哲学"）改动了编辑的原措辞，待编辑二审确认。
2. **已删内容的取舍**：演进时间线表、HC OLMo 实验表、GRPO 超参/常见问题表均可从 git 历史（f3cedef 及更早）恢复，若作者想保留其中某些，可考虑移入附录。
3. `.review_state.json` 是此前 Codex 审校桥工具的状态文件，当前流程不再使用，暂保留。
