# 修订稿修改记录

> **2026-08-23 已回灌 Markdown 源**：下列全部内容修改与格式修复已合并进 `chapter2_new.md` / `chapter3_new.md`（详见文末"回灌说明"），Markdown 重新成为唯一权威源，交稿 docx 此后一律由 `md2docx.py` 重新生成。两个"公式修订稿"docx 存在缺陷（见文末），仅作历史参考，勿再在其上继续修改。



## 第 3 章（内容修改 18 项）

1. **PPO 目标**：采样分布由 π_θ 改为 π_θ_old。
2. **GRPO 目标**：补旧策略 π_θ_old 采样、1/G 组平均、−β·D_KL(π_θ‖π_ref) 项；说明段同步改为"从旧策略采样"并补 β、π_ref 释义。
3. **优势函数**：奖励记号 r(x,y_i) 改为 R(x,y_i)（避免与重要性比率 r_i(θ) 同字母冲突），并加注"ε 为数值稳定项、原文无此项"。
4. **KV-cache**：公式改为 M_KV=2·G·L·H_KV·d_h·T·b（补 K/V 因子 2、H 改 KV 头数）；Llama-3.1-8B 重算为约 20 GiB；注明表 3-40 的 68 GB 沿用 Unsloth 博客口径、与上式口径不同；前文 M_kv 段"注意力头数"改为"KV 头数"。
5. **FlashAttention 反向**：∂L/∂Q、∂L/∂K 补乘 τ=1/√d_k，并在梯度链说明中补 τ 定义。
6. **SRAM 约束**：改为论文形式 B_c=⌈M_SRAM/(4d)⌉、B_r=min(⌈M_SRAM/(4d)⌉, d)。
7. **DAPO Clip-Higher**：改为统一区间 [1−ε_low, 1+ε_high]，删除按优势符号交换的分段写法；参数 0.1/0.3 改为 0.2/0.28。
8. **DAPO Dynamic Sampling**：改写为"过滤组内全对/全错 prompt（正确响应数严格位于 0 与 G 之间）"，删除 clip_fraction>0.9 说法。
9. **DAPO Token-Level Loss**：改写为"聚合分母由每序列先平均改为组内 token 总数"，删除 1/2/3 阶段权重。
10. **DAPO Overlong**：改为分段软惩罚（0 / 线性 [(L_max−L_cache)−|y|]/L_cache / −1），参数改期望 16384、L_cache=4096、L_max=20480，并补 Overlong Filtering 说明。
11. **StepGRPO 总式**：改为 r^i=r_RAR^i+r_RVR^i 直接相加，说明 α=0.1、β=0.04 的真实含义。
12. **StepRAR**：改为论文式 k^i=|v_match|/|v| 与分段奖励（1+αk^i / αk^i / 0）。
13. **StepRVR**：改为完整性∧逻辑性两条件取 1。
14. **删除表 3-45**（StepGRPO 与 GRPO 的 GSM8K/MATH 对比，数据无出处），正文改为 R1-VL 真实实验（Qwen2-VL 系列、MathVista 等 9 个多模态基准）的定性表述。
15. **作者推导标注 7 处**：DeepGEMM 二级累加、3FS chunk_id 与租约式、DeepEP 通信量与解码延迟、FlashMLA FLOPs/带宽、FP8 2 的幂缩放。

## 第 3 章（格式修复）

- 指数底数 e 改正体 10 处；`\mathcal` 误用修复 19 处（在线 Softmax 的 ℓ、`x∼𝒟,{`、`∈𝒱`、`ℒ=` 等）；定向补加粗（粗斜体）22 处（GEMM 的 A/B/C、FA 的 Q/K/V/P/O/S、FlashMLA、DSpark 的 W/h/z/s 等）。

## 第 2 章（内容修改 9 项）

1. **CSA 分组输出投影**：公式补为两级——各组 o^G_{t,i}·W_i^G 后 Concat，再乘 W^O 投影回隐藏维度（与正文已有的"拼接 16384 维再投影回 7168"描述一致）。
2. **注意力汇**：公式改 s_{sink,h}，正文注明"每个查询头各自的可学习标量"。
3. **HCA 缓存估算**：补"末尾不足一块（约 80 token）的 pending 状态需保留在 state cache"的说明。
4. **mHC 前向流程（前段）**：改写为与原文一致的形式——Ã_l/C̃_l/B̃_l 三组无约束系数；A_l=σ(Ã_l)、C_l=2σ(C̃_l)、B'_l=exp(B̃_l)；仅残差映射 B_l=Sinkhorn-Knopp(B'_l, T=20) 投影到双随机流形；前向式 X_{l+1}=B_lX_l+C_lF_l(A_lX_l)。消除与后段的冲突。
5. **"能量守恒"**：性质 4 改为"总和保持与非扩张"（行/列和为 1、谱范数 ≤1、ℓ₂ 非扩张）。
6. **Birkhoff 体积比式**：标注"该极限式为直观推导，非 mHC 原文结论"。
7. **MLA 矩阵吸收**：注明"最早在 DeepSeek-V2 中提出"。
8. **GRPO-thinking 式**：标注"示意性目标；V3.2 原文长度惩罚在奖励侧"。
9. **HC 的 1.05^60**：标注"举例演算"。

## 第 2 章（格式修复）

- 粗正体→粗斜体 374 处（保留 1 处加粗数字 0，属零块矩阵惯例）；`\mathcal`/`\mathbb` 误用修复 31 处（`\mathbb{∈R}`、`\mathbb{=E}`、`\mathbb D_KL`、`\mathcal l`、`\mathcal{⋅U}`、`\mathcal{HC=}` 等）；说明型标签与函数名正体 88 处（old/ref/KV/DKV/GRPO/Bal/win/sink/log/min 等）；指数底数 e 正体 1 处；定向补加粗 43 处（MLA/CSA/MTP/Muon/HC/mHC 各公式中漏加粗的 W、c、h、q、k、Z、M 等）。


## 回灌说明（2026-08-23，docx 修订并回 Markdown 源）

上述修订当时直接改在 docx 上，Markdown 源一度落后。本次已将全部修改回灌至 `chapter2_new.md` / `chapter3_new.md`，要点与决定如下：

### 黑斜体约定
- Markdown 中黑斜体一律用 `\mathbfit{}`（pandoc 原生输出 OMML `sty="bi"`，Word 显示粗斜体）；第 3 章原本就用此写法，第 2 章本次将 375 处 `\mathbf`（Word 里显示粗正体，即 Peter 指出的问题）统改为 `\mathbfit`，仅保留 1 处 `\mathbf{0}`（零块矩阵惯例）。
- 顺带修正原稿 58 处**错误加粗的标量 k**（DSA 的 Top-k 数量、MTP 深度索引），改回普通斜体。

### 发现的"公式修订稿"docx 缺陷（回灌时已纠正，未照搬）
1. **第 2 章 docx 黑斜体大量丢失**：修改记录声称"粗正体→粗斜体 374 处"，但该 docx 仅 35 个 bi 样式运行（集中在 8 个新改写公式），其余全部退化为普通斜体；`\mathbb`/`\mathcal` 也几乎全丢（全篇仅存 6 个花体/黑板体运行）。系其重建工具丢样式所致。Markdown 按修改记录的**意图**落实，不按该 docx 的实际样式。
2. **Peter 批注问句误入正文**：2.7.4 小标题被并入"（Kimi K3 近几天刚出来，要不要替换成 Kimi K3 的叙述？）"——这是 Peter 的批注原文，不应入正文，未采纳。
3. **Peter 已定的措辞被回退**：两处"增至约 8 倍"在该 docx 中退回"增长约 8 倍"（违反 RULE 31），未采纳。
4. **第 3 章表号退回旧编号**：该 docx 基于 Peter 原稿重建，表号仍为删重复表前的 3-21～3-45（有缺号）；Markdown 保持已完成的连续编号，删除 StepGRPO 对比表（原表 3-44）后全章表号为 3-1～3-43 连续无缺号。
5. **GRPO 重要性比率定义错误**：该 docx 中 r_i(θ) 分子分母均为 π_θ_old（恒等于 1），未采纳，保持 π_θ/π_θ_old。

### 回灌时的少量技术性偏离（均更符合规范/原文）
- 优势函数保留估计量帽子 Â_i（docx 中帽子丢失系转换工件，正文"帽子符号表示估计量"的解释仍在）。
- DSpark 置信度 c_k 为标量，保持非粗（docx 误加粗）。
- 第 2 章 \mathcal{L}（损失）、\mathcal{S}、\mathcal{B}_n、\mathcal{R}、\mathcal{T}、\mathcal{F}、\mathcal{HC} 等合法花体保留（与第 3 章 docx 保留 ℒ/𝒟/𝒱 的口径一致）；仅 \mathcal{J}_PPO/GRPO→J、\mathbb{D}_KL→D_KL、`\mathbb{∈R}`/`\mathbb{=E}` 范围误用按记录修正。
- 作者推导标注的插入位置放在公式/段落语义完整处（docx 工具是机械追加在冒号后，如"保证：（该式为示意性模型…）"）。
- mHC"信号增益"小节的 G_L=∏W_l 同步改为 ∏B_l，与新前向记号一致（彻底消除前后段记号冲突）。

### 其他统改（记录之外、按既有 RULE 补做）
- 第 2 章说明型标签正体 85 处（^{KV}/^{DKV}/^{UK}/^{UV}/^{DQ}/^{UQ}/_{old}/_{ref}/_{Bal}/_{KL}/_{layers}/_{bytes} 等→\text{}）；e 正体 1 处；`min` 补 `\min` 1 处。
- 第 2 章 CSA 压缩/索引器公式补粗斜体（Z/S/B/c/h/q/K/W）、DSA 索引键 k̄、Muon 全节矩阵符号（M/G/U/V/W/O/Σ）——即修改记录"定向补加粗"在 Markdown 的落实。
- ℓ→l 全书统改（第 2 章 21 处、第 3 章 15 处，Word 花体 ℓ 字形问题）。
- `md2docx.py` 关闭 pandoc 的 `autolink_bare_uris`：中文紧邻 "pass@1" 会被误识别为邮箱生成悬空超链接（此前交稿 docx 均带此瑕疵）；脚注裸 URL 改为纯文本呈现，更符合印刷需要。

### 验证
- `python3 md2docx.py` 两章公式校验通过（第 2 章行间 103/行内 731，第 3 章行间 64/行内 524，OMML 数目一致）；第 2 章生成 docx 含 442 个 bi（粗斜体）运行、仅 1 个 b（粗正体，零矩阵）。
- 新 docx 往返转换与"公式修订稿"docx 逐行归一化对比，除上述已说明的缺陷项与转换噪音外无实质差异，全部 18+9 项内容修改均已落地。
